.next
.history
.husky
node_modules
public

# Written by `pnpm build:chat-corpus`. Committed so the corpus the bot reads is
# reviewable in a diff, but formatting it would only create noise.
src/generated
